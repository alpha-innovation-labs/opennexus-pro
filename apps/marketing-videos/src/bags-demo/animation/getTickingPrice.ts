/**
 * Calculates a small live-ticking display price for the selected token.
 *
 * @param frame Local scene frame.
 * @returns Formatted token price.
 */
export function getTickingPrice(frame: number): string {
  return "$1.60";
}
