/**
 * Renders the compact footer for the landing page.
 *
 * @returns Static HTML for the footer.
 */
export function renderFooter(): string {
  return `
    <footer class="site-footer">
      <p>Nexus is built for focused agentic engineering.</p>
      <a href="#top">Back to top</a>
    </footer>
  `;
}
