# gateway/commands

- Source path: `src/gateway/commands`
- Parent: [`gateway`](../gateway.md)

## What this area covers

`src/gateway/commands` is the gateway `commands` slice. It is a leaf folder with 5 files and no nested directories.

## Key files

- `src/gateway/commands/startGateway.ts`
- `src/gateway/commands/getGatewayStatus.ts`
- `src/gateway/commands/formatGatewayStatus.ts`
- `src/gateway/commands/restartGateway.ts`
- `src/gateway/commands/stopGateway.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/gateway/commands/startGateway.ts`
2. `src/gateway/commands/getGatewayStatus.ts`
3. `src/gateway/commands/stopGateway.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
