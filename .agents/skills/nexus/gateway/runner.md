# gateway/runner

- Source path: `src/gateway/runner`
- Parent: [`gateway`](../gateway.md)

## What this area covers

`src/gateway/runner` is the gateway `runner` slice. It is a leaf folder with 4 files and no nested directories.

## Key files

- `src/gateway/runner/runGatewayDaemon.ts`
- `src/gateway/runner/runGatewayServices.ts`
- `src/gateway/runner/writeGatewayHeartbeat.ts`
- `src/gateway/runner/ensureGatewayRootDir.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/gateway/runner/runGatewayDaemon.ts`
2. `src/gateway/runner/runGatewayServices.ts`
3. `src/gateway/runner/ensureGatewayRootDir.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
