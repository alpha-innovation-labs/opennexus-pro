# gateway/state

- Source path: `src/gateway/state`
- Parent: [`gateway`](../gateway.md)

## What this area covers

`src/gateway/state` is the gateway `state` slice. It is a leaf folder with 6 files and no nested directories.

## Key files

- `src/gateway/state/createGatewayState.ts`
- `src/gateway/state/readGatewayHeartbeat.ts`
- `src/gateway/state/readGatewayState.ts`
- `src/gateway/state/writeGatewayState.ts`
- `src/gateway/state/clearGatewayState.ts`
- `src/gateway/state/types.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/gateway/state/createGatewayState.ts`
2. `src/gateway/state/readGatewayHeartbeat.ts`
3. `src/gateway/state/readGatewayState.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
