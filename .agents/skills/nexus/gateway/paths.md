# gateway/paths

- Source path: `src/gateway/paths`
- Parent: [`gateway`](../gateway.md)

## What this area covers

`src/gateway/paths` is the gateway `paths` slice. It is a leaf folder with 4 files and no nested directories.

## Key files

- `src/gateway/paths/getGatewayHeartbeatPath.ts`
- `src/gateway/paths/getGatewayLogPath.ts`
- `src/gateway/paths/getGatewayRootPath.ts`
- `src/gateway/paths/getGatewayStatePath.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/gateway/paths/getGatewayHeartbeatPath.ts`
2. `src/gateway/paths/getGatewayLogPath.ts`
3. `src/gateway/paths/getGatewayRootPath.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
