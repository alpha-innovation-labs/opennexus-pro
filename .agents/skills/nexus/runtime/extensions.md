# runtime/extensions

- Source path: `src/runtime/extensions`
- Parent: [`runtime`](../runtime.md)

## What this area covers

`src/runtime/extensions` is the runtime `extensions` slice. It is a leaf folder with 1 file and no nested directories.

## Key files

- `src/runtime/extensions/resolveBundledExtensionFactories.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/runtime/extensions/resolveBundledExtensionFactories.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
