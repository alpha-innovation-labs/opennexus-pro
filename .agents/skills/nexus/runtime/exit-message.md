# runtime/exit-message

- Source path: `src/runtime/exit-message`
- Parent: [`runtime`](../runtime.md)

## What this area covers

`src/runtime/exit-message` is the runtime `exit-message` slice. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/runtime/exit-message/registerExitMessageProcessHandler.ts`
- `src/runtime/exit-message/printExitMessage.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/runtime/exit-message/registerExitMessageProcessHandler.ts`
2. `src/runtime/exit-message/printExitMessage.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
