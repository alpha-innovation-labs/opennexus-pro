# runtime/clipboard-image

- Source path: `src/runtime/clipboard-image`
- Parent: [`runtime`](../runtime.md)

## What this area covers

`src/runtime/clipboard-image` is the runtime `clipboard-image` slice. It is a leaf folder with 3 files and no nested directories.

## Key files

- `src/runtime/clipboard-image/readClipboardImageViaMacOsJxa.ts`
- `src/runtime/clipboard-image/writeClipboardImageTempFile.ts`
- `src/runtime/clipboard-image/types.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/runtime/clipboard-image/readClipboardImageViaMacOsJxa.ts`
2. `src/runtime/clipboard-image/writeClipboardImageTempFile.ts`
3. `src/runtime/clipboard-image/types.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
