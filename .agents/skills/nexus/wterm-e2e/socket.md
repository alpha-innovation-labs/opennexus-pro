# wterm-e2e/socket

- Source path: `apps/wterm-e2e/src/socket`
- Parent: [`wterm-e2e`](../wterm-e2e.md)

## What this area covers

`apps/wterm-e2e/src/socket` is the `socket` side of the web-terminal demo. It is a leaf folder with 1 file and no nested directories.

## Key files

- `apps/wterm-e2e/src/socket/createTerminalSocket.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `apps/wterm-e2e/src/socket/createTerminalSocket.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
