# extensions/generated

- Source path: `src/extensions/generated`
- Parent: [`extensions`](../extensions.md)

## What this area covers

`src/extensions/generated` is the `generated` subarea of `extensions`. It is a leaf folder with 1 file and no nested directories.

## Key files

- `src/extensions/generated/registerCompiledEnabledExtensions.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/extensions/generated/registerCompiledEnabledExtensions.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
