# adapters/shared

- Source path: `src/adapters/shared`
- Parent: [`adapters`](../adapters.md)

## What this area covers

`src/adapters/shared` is the `shared` adapter area. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/adapters/shared/listBuiltInAdapters.ts`
- `src/adapters/shared/types.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/adapters/shared/listBuiltInAdapters.ts`
2. `src/adapters/shared/types.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
