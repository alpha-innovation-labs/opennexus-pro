# adapters/discord

- Source path: `src/adapters/discord`
- Parent: [`adapters`](../adapters.md)

## What this area covers

`src/adapters/discord` is the `discord` adapter area. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/adapters/discord/index.ts`
- `src/adapters/discord/createDiscordAdapterDefinition.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/adapters/discord/index.ts`
2. `src/adapters/discord/createDiscordAdapterDefinition.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
- `src/adapters/discord/createDiscordAdapterDefinition.ts` currently returns stage `planned`.
