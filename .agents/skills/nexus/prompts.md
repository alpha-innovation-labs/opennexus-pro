# prompts

- Source path: `src/prompts`
- Skill index: [`SKILL.md`](./SKILL.md)

## What this area covers

`src/prompts` holds bundled prompt assets appended to Nexus runs. This top level is organized entirely by 1 immediate component folder.

## Root entry files

This folder has no root-level files.

## Component map

- [`base-system-prompt`](./prompts/base-system-prompt.md) — files: `baseSystemPrompt.ts`

## Read this first

1. `src/prompts/base-system-prompt/baseSystemPrompt.ts`

## Navigation notes

- Start with the root entry files when you need the boundary or registration flow, then jump into the component that matches the feature you are touching.
- Use the component map above as the one-level drill-down for this folder; deeper structure stays inside each component doc.
- `src/prompts/base-system-prompt/baseSystemPrompt.ts` contains the literal Nexus system-prompt block appended to runs.
