# feature-flags/generated

- Source path: `src/feature-flags/generated`
- Parent: [`feature-flags`](../feature-flags.md)

## What this area covers

`src/feature-flags/generated` stores generated feature-flag artifacts. It is a leaf folder with 1 file and no nested directories.

## Key files

- `src/feature-flags/generated/compiledFeatureFlags.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/feature-flags/generated/compiledFeatureFlags.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
- `src/feature-flags/generated/compiledFeatureFlags.ts` is the build-time snapshot consumed by compiled/release code paths.
