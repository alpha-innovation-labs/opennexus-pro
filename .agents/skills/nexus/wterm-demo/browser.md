# wterm-demo/browser

- Source path: `src/wterm-demo/browser`
- Parent: [`wterm-demo`](../wterm-demo.md)

## What this area covers

`src/wterm-demo/browser` is the `browser` side of the web-terminal demo. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/wterm-demo/browser/index.ts`
- `src/wterm-demo/browser/createTerminalApp.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/wterm-demo/browser/index.ts`
2. `src/wterm-demo/browser/createTerminalApp.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
