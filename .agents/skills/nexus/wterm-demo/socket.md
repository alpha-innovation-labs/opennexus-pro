# wterm-demo/socket

- Source path: `src/wterm-demo/socket`
- Parent: [`wterm-demo`](../wterm-demo.md)

## What this area covers

`src/wterm-demo/socket` is the `socket` side of the web-terminal demo. It is a leaf folder with 1 file and no nested directories.

## Key files

- `src/wterm-demo/socket/createTerminalSocket.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/wterm-demo/socket/createTerminalSocket.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
