# pi-slash-usage/runtime

- Source path: `src/pi-slash-usage/runtime`
- Parent: [`pi-slash-usage`](../pi-slash-usage.md)

## What this area covers

`src/pi-slash-usage/runtime` is the `runtime` layer of the slash-usage extension. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/pi-slash-usage/runtime/refreshUsageForContext.ts`
- `src/pi-slash-usage/runtime/shouldRefreshUsage.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/pi-slash-usage/runtime/refreshUsageForContext.ts`
2. `src/pi-slash-usage/runtime/shouldRefreshUsage.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
