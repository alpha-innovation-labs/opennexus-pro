# cli/adapter

- Source path: `src/cli/adapter`
- Parent: [`cli`](../cli.md)

## What this area covers

`src/cli/adapter` is the CLI `adapter` slice. It is a leaf folder with 4 files and no nested directories.

## Key files

- `src/cli/adapter/runAdapterCommand.ts`
- `src/cli/adapter/printAdapterUsage.ts`
- `src/cli/adapter/isAdapterCommand.ts`
- `src/cli/adapter/isGatewayRunnerCommand.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/cli/adapter/runAdapterCommand.ts`
2. `src/cli/adapter/printAdapterUsage.ts`
3. `src/cli/adapter/isAdapterCommand.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
