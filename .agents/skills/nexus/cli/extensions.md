# cli/extensions

- Source path: `src/cli/extensions`
- Parent: [`cli`](../cli.md)

## What this area covers

`src/cli/extensions` is the CLI `extensions` slice. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/cli/extensions/hasNoExtensionsFlag.ts`
- `src/cli/extensions/appendNoExtensionsArg.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/cli/extensions/appendNoExtensionsArg.ts`
2. `src/cli/extensions/hasNoExtensionsFlag.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
