# cli/system-prompt

- Source path: `src/cli/system-prompt`
- Parent: [`cli`](../cli.md)

## What this area covers

`src/cli/system-prompt` is the CLI `system-prompt` slice. It is a leaf folder with 2 files and no nested directories.

## Key files

- `src/cli/system-prompt/hasBaseSystemPromptArg.ts`
- `src/cli/system-prompt/addBaseSystemPromptArg.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/cli/system-prompt/addBaseSystemPromptArg.ts`
2. `src/cli/system-prompt/hasBaseSystemPromptArg.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
