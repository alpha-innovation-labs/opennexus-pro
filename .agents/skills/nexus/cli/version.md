# cli/version

- Source path: `src/cli/version`
- Parent: [`cli`](../cli.md)

## What this area covers

`src/cli/version` is the CLI `version` slice. It is a leaf folder with 3 files and no nested directories.

## Key files

- `src/cli/version/readCliPackageVersion.ts`
- `src/cli/version/printAppVersion.ts`
- `src/cli/version/hasVersionFlag.ts`

## Immediate subareas

This folder has no nested directories.

## Read this first

1. `src/cli/version/printAppVersion.ts`
2. `src/cli/version/readCliPackageVersion.ts`
3. `src/cli/version/hasVersionFlag.ts`

## Navigation notes

- Start with the first listed file to find the public entrypoint, registration boundary, or top-level contract for this area.
- Because this is a leaf folder, the root files are the whole implementation surface for this layer.
