/** Minimum RTK savings modal width. */
export const RTK_SAVINGS_MODAL_MIN_WIDTH = 56;

/** Maximum RTK savings modal width. */
export const RTK_SAVINGS_MODAL_MAX_WIDTH = 72;

/** Maximum RTK savings modal terminal-width ratio. */
export const RTK_SAVINGS_MODAL_MAX_WIDTH_RATIO = 0.8;
