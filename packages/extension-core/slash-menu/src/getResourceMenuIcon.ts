/**
 * Returns a stable single-cell icon for resource menus.
 *
 * @returns Navigation icon used for prompt and skill resource rows.
 */
export function getResourceMenuIcon(): string {
  return "›";
}
