/**
 * Mirrors the current Nexus session title into the active cmux surface title.
 *
 * @param sessionTitle Current Nexus session title.
 */
export declare function syncCmuxPaneTitle(sessionTitle: string): Promise<void>;
