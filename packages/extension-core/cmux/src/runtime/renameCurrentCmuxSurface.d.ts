/**
 * Renames the current cmux surface title.
 *
 * @param title New surface title.
 * @returns True when cmux accepted the rename request.
 */
export declare function renameCurrentCmuxSurface(title: string): Promise<boolean>;
