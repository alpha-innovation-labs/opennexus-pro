/**
 * Executes a cmux CLI command.
 *
 * @param args Command arguments passed to cmux.
 * @returns True when cmux exits successfully.
 */
export declare function runCmuxCommand(args: string[]): Promise<boolean>;
