/**
 * Builds the compact todo hotkey hint shown below the input.
 *
 * @returns One-line todo hotkey hint.
 */
export function buildTodoHintText(): string {
	return "… j/k move … a add … e edit … x done … dd delete … ? help … esc back … ctrl+c hide …";
}
