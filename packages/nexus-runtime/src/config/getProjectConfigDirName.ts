const PROJECT_CONFIG_DIR_NAME = ".nexus";

/**
 * Returns the Nexus project config directory name.
 *
 * @returns Project config directory name.
 */
export function getProjectConfigDirName(): string {
	return PROJECT_CONFIG_DIR_NAME;
}
