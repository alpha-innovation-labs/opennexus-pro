/**
 * Splits the current pane to the right and returns the new pane ID.
 *
 * @returns The pane_id of the newly split pane.
 * @throws If the split fails or returns no pane_id.
 */
export declare function splitPaneRight(): string;
