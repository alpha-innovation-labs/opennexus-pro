/**
 * Closes a Herdr workspace, cleaning up all its panes and agents.
 *
 * @param workspaceId The workspace ID to close (e.g. "w42").
 */
export declare function closeHerdrWorkspace(workspaceId: string): void;
