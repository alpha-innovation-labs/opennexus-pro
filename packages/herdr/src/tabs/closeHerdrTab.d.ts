/**
 * Closes a Herdr tab, cleaning up its panes and agents.
 * Mirrors `herdr tab close`.
 *
 * @param tabId The tab ID to close (e.g. "w42:t1").
 */
export declare function closeHerdrTab(tabId: string): void;
