# Pi Annotate source

Source repository: https://github.com/nicobailon/pi-annotate

Notes:
- The Pi extension logic from the upstream `index.ts` was imported into `src/extensions/annotate/` and split into one-function-per-file modules for this project.
- The upstream Chrome extension folder was copied into `src/chrome-extension/`.
