/**
 * Returns the key prefix used for terminal variable entries.
 *
 * @returns Terminal variable entry prefix.
 */
export function getTermVariableEntryPrefix(): string {
	return "extension.term.variables.";
}
