/**
 * Returns the key prefix used for named terminal shortcut entries.
 *
 * @returns Terminal shortcut entry prefix.
 */
export function getTermShortcutEntryPrefix(): string {
	return "extension.term.shortcuts.";
}
