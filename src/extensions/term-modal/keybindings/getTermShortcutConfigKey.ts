/**
 * Returns the custom keybindings.json key used for toggling the terminal modal.
 *
 * @returns Extension keybinding config key.
 */
export function getTermShortcutConfigKey(): string {
	return "extension.term.toggle";
}
