const DEFAULT_THEME_NAME = "nexus-black";

/**
 * Returns the default Nexus theme name.
 *
 * @returns Default theme name.
 */
export function getDefaultThemeName(): string {
  return DEFAULT_THEME_NAME;
}
