1. Setup deep research
    - it needs to be comprehensive
    - it must store previous researches in a global location
    - it must be able to retrieve other researches to check first if such a research exists -- maybe rag style?
2. Bring back the code to skill, skill, and run it on this codebar, then explore using it instead of rag or other stuff to do work
    - if that works, we then need a workflow that takes a summary of conversations and code and makes sense out of them in the original skills


# Topics
## Observational memory
- Is it possible to rewrite the context?
- What if we updated the tool calls so that they don't store the entire context when requested by the llm? So for exampe, running a certain command would get washed out of context after it was read for example


- Use this link as sample of HUGE web_fetch result. we should truncate it https://registry.npmjs.org/react
