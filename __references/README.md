# References

This folder contains preserved and external subagent references used during the rewrite.

## Contents

1. `current-sub-agents/` — snapshot of the Nexus subagent implementation that was removed from active source.
2. `tintinweb-pi-subagents/` — git submodule for `https://github.com/tintinweb/pi-subagents`.
3. `nicobailon-pi-subagents/` — git submodule for `https://github.com/nicobailon/pi-subagents`.
