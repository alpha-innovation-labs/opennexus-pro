┌───────────────────────┬──────────────────────────────────┐
│ Provider              │ Status                           │
├───────────────────────┼──────────────────────────────────┤
│ lm-studio             │ 1 models found                   │
│ docker-model-runner   │ Nothing is running on port 8000  │
│ exllamav2             │ Nothing is running on port 5000  │
│ gpt4all               │ Nothing is running on port 4891  │
│ h2ogpt                │ Nothing is running on port 5000  │
│ harbor                │ Nothing is running on port 3000  │
│ jan                   │ Nothing is running on port 1337  │
│ koboldcpp             │ Nothing is running on port 5001  │
│ lemonade              │ Nothing is running on port 8080  │
│ litellm               │ Nothing is running on port 4000  │
│ llama.cpp             │ Nothing is running on port 8080  │
│ llamafile             │ Nothing is running on port 8080  │
│ lmddeploy             │ Nothing is running on port 23333 │
│ localai               │ Nothing is running on port 8080  │
│ mlx-lm                │ Nothing is running on port 8080  │
│ mlx-openai-server     │ Nothing is running on port 8080  │
│ ollama                │ Nothing is running on port 11434 │
│ omlx                  │ Nothing is running on port 8000  │
│ open-webui            │ Nothing is running on port 3000  │
│ openllm               │ Nothing is running on port 3000  │
│ sglang                │ Nothing is running on port 30000 │
│ tensorrt-llm          │ Nothing is running on port 8000  │
│ text-generation-webui │ Nothing is running on port 7860  │
│ vllm                  │ Nothing is running on port 8000  │
└───────────────────────┴──────────────────────────────────┘
