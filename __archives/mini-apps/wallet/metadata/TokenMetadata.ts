/**
 * Display metadata for one SPL token mint.
 */
export interface TokenMetadata {
	mint: string;
	symbol?: string;
	name?: string;
	imageUrl?: string;
	imageBase64?: string;
	imageMimeType?: string;
}
