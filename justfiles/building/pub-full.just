# Uninstall, publish, reinstall, and launch the npm package end-to-end.
# Usage: just pub-full
# Example: just pub-full

# NOTE: This recipe is a stub. It has been removed from the justfile.
# To re-enable, import pub-full.just back into the root justfile.

pub-full:
    #!/usr/bin/env bash
    set -euo pipefail

    # STUB: All release script references have been removed.
    # To re-enable, restore the original recipe body.
