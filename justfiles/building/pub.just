# Build the npm release package and publish it to npmjs from the repo root.
# Usage: just pub
# Example: just pub

# NOTE: This recipe is a stub. It has been removed from the justfile.
# To re-enable, import pub.just back into the root justfile.

pub:
    #!/usr/bin/env bash
    set -euo pipefail

    # STUB: All release script references have been removed.
    # To re-enable, restore the original recipe body.
