# ============================================================================
# Sync vendor source (pi) from ../pi clone and rebuild CodeGraph index.
# ============================================================================

sync-vendor:
    @bash scripts/sync-vendor-pi.sh
